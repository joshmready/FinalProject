@model IEnumerable<Microsoft.AspNet.Identity.EntityFramework.IdentityRole>

@{
    ViewBag.Title = "List of User Types";
}
<hr/>
<h2>@ViewBag.Title</h2>
<hr/>

@*<p>
    @Html.ActionLink("Create New", "Create", "Reservations", null, new { @class = "button btn btn-primary d-none d-sm-block btn-style mr-2" })
</p>*@
<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "button btn btn-primary d-none d-sm-block btn-style mr-2" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>

        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
